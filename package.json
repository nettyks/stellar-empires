{
  "name": "stellar-empires",
  "version": "0.1.0",
  "description": "Jeu de stratégie, économie et gestion multijoueur dans l'espace",
  "scripts": {
    "build": "cd server && npm install && npm run build",
    "start": "node server/dist/index.js"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
